import 'package:flutter/material.dart';

const primaryColor = Color(0xFF42A5F5); // Light Blue for sports theme
const accentColor = Color(0xFFFFA726); // Orange accent

Widget buildTopIcon() {
  return const Icon(
    Icons.sports_soccer, // Placeholder sports icon
    color: primaryColor,
    size: 80,
  );
}
